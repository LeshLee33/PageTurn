{
  "dependencies": {
    "axios": "^1.7.7",
    "react-scripts": "^5.0.1"
  },
  "name": "your-project-name",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd src/frontend && react-scripts start",
    "build": "cd src/frontend && react-scripts build",
    "test": "cd src/frontend && react-scripts test",
    "eject": "cd src/frontend && react-scripts eject",
    "server": "python src/server/main.py",
    "dev": "concurrently \"npm start\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^6.5.1"
  }
}
